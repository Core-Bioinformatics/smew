% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_bulk.R
\name{bulk_utils_run_ORA}
\alias{bulk_utils_run_ORA}
\title{Over-Representation Analysis (ORA) Helper Function (Bulk)}
\usage{
bulk_utils_run_ORA(
  pathways,
  metabolites,
  universe,
  minSize = 1,
  maxSize = length(universe) - 1,
  direction = c("up", "down")
)
}
\arguments{
\item{pathways}{A named list where each element is a character vector of metabolites in a pathway.}

\item{metabolites}{Character vector of metabolites of interest (e.g., DE peaks).}

\item{universe}{Character vector of all metabolites considered as background/universe.}

\item{minSize}{Minimum pathway size to include (default: 1).}

\item{maxSize}{Maximum pathway size to include (default: length(universe) - 1).}

\item{direction}{Character string or vector, e.g. 'up' or 'down', indicating direction of regulation (added to output).}
}
\value{
Data frame with columns: pathway, total, expected, hits, Raw.p, Holm.p, FDR, metabolites, direction.
}
\description{
Computes pathway enrichment for a set of metabolites using the hypergeometric test.
Filters pathways by size and returns a data frame of enrichment statistics for each pathway.
}
\details{
Used internally by the ORA Shiny panel. Pathways with no overlap or outside size limits are excluded.
}
\keyword{internal}
