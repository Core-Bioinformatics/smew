% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_region.R
\name{region_utils_create_bulk_exp_regions}
\alias{region_utils_create_bulk_exp_regions}
\title{Create pseudobulk region-level expression matrix}
\usage{
region_utils_create_bulk_exp_regions(
  intensity.matrix,
  metadata,
  bulk.metadata,
  sample.id.metadata.column,
  region.ids,
  minimum.pixels = 3
)
}
\arguments{
\item{intensity.matrix}{Data frame of intensity values (features x samples)}

\item{metadata}{Data frame of sample metadata (must include 'Sample' column)}

\item{bulk.metadata}{Data frame of bulk sample metadata}

\item{sample.id.metadata.column}{Name of the sample ID column in metadata}

\item{region.ids}{Vector of region IDs for each sample (length must match nrow(metadata))}

\item{minimum.pixels}{Minimum number of pixels per region per sample to be considered (default 3)}
}
\value{
A list with:
  \item{intensity.matrix}{Matrix of aggregated intensities (features x pseudobulked samples)}
  \item{metadata}{Data frame of metadata for pseudobulked samples}
}
\description{
Aggregates intensity values by user-defined regions and samples, producing a pseudobulk expression matrix and corresponding metadata for region-level differential analysis.
}
\details{
For each unique region within each sample, the function computes the mean intensity across all pixels in that region, provided the region contains at least \code{minimum.pixels} pixels. The resulting matrix and metadata are suitable for downstream region-level differential analysis.
}
\keyword{internal}
