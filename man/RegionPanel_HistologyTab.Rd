% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regionPanel_HistologyTab.R
\name{RegionPanel_HistologyTab}
\alias{RegionPanel_HistologyTab}
\alias{RegionPanel_HistologyTabUI}
\alias{RegionPanel_HistologyTabServer}
\title{Allows users to manually select regions of interest, optionally using histology images}
\usage{
RegionPanel_HistologyTabUI(
  id,
  bulk.metadata,
  full.metadata,
  full.intensity.matrix,
  show = TRUE
)

RegionPanel_HistologyTabServer(
  id,
  full.intensity.matrix,
  full.metadata,
  bulk.metadata,
  anno,
  shared_data
)
}
\arguments{
\item{id}{Shiny module id (for both UI and server)}

\item{bulk.metadata}{Data frame of bulk sample metadata (UI)}

\item{full.metadata}{Data frame of full sample metadata (UI, server)}

\item{full.intensity.matrix}{Matrix of intensities (features x samples, UI, server)}

\item{show}{Logical; whether to show the panel (default TRUE, UI)}

\item{anno}{Data frame of peak annotations (server)}

\item{shared_data}{Reactive or shared data object (server)}
}
\value{
UI: A shiny tabPanel object for the manual region selection tab. Server: None (side effects in Shiny module).
}
\description{
UI and server logic for the Manual Region Selection and Histology Overlay panel, enabling users to overlay metabolic data with histology images, draw custom regions of interest, and export region/cluster assignments in spatial omics data. Supports sample selection, overlay customisation, and download of results.
}
\details{
\itemize{
  \item{Overlay metabolic information with histology images (if available) and manually draw custom regions of interest.}
  \item{Customise cluster/region names.}
  \item{Select samples to display and interactively define regions.}
  \item{Export region or cluster assignments for downstream analysis.}
  \item{All results and overlays are available for download.}
}
}
