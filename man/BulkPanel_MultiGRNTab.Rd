% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bulkPanel_MultiGRNTab.R
\name{BulkPanel_MultiGRNTab}
\alias{BulkPanel_MultiGRNTab}
\alias{BulkPanel_MultiGRNTabUI}
\alias{BulkPanel_MultiGRNTabServer}
\title{Infers multi-omics covariation networks}
\usage{
BulkPanel_MultiGRNTabUI(id, bulk.metadata, show = TRUE)

BulkPanel_MultiGRNTabServer(
  id,
  bulk.intensity.matrix,
  bulk.metadata,
  anno,
  multi_modal
)
}
\arguments{
\item{id}{Shiny module id (for both UI and server)}

\item{bulk.metadata}{Data frame with bulk sample metadata}

\item{show}{Logical; whether to render the panel (default: TRUE)}

\item{bulk.intensity.matrix}{Numeric matrix of bulk sample intensities (features × samples)}

\item{anno}{Data frame with peak annotation (must include 'display_name' and 'm_z')}

\item{multi_modal}{Numeric matrix of multi-modal data (features × samples)}
}
\value{
A shiny::tabPanel containing the UI elements for the multi-modal GRN panel
}
\description{
UI and server logic for comparative GRN inference across multiple sample groups using combined bulk and multi-modal data. Enables side-by-side comparison of metabolite peak regulatory relationships across different conditions or sample sets.
}
\details{
\itemize{
  \item{Multi-network inference (1-4 networks) from independent sample subsets}
  \item{GENIE3-based GRN construction from combined bulk/multi-modal data}
  \item{Network visualization with recurring regulators highlighted}
  \item{Interactive upset plots showing regulator overlap across networks}
  \item{Customizable connection filtering and network topology visualization}
  \item{Accepts bulk intensity matrix and multi-modal data, combining them for joint GRN inference. Removes constant features before analysis. Supports flexible sample selection per network and identifies regulatory patterns that are conserved or divergent across conditions.}
}
}
